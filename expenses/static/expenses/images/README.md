# Background image folder

Put your custom wallpaper image in this folder and update `expenses/static/expenses/css/style.css`:

```css
:root {
  --wallpaper-image: url('../images/your-image-file.jpg');
}
```

The stylesheet already applies blur and a fixed background effect.
